{
  "name": "dev-sahad-profile",
  "version": "1.0.0",
  "private": true,
  "description": "Automation and validation tooling for the Dev-Sahad GitHub profile.",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check": "node scripts/validate-profile.js && node scripts/analyze-profile-image.js --check && node scripts/make-terminal-svg.js --check && node scripts/update-readme-date.js --check",
    "generate": "npm run generate:pixel && npm run generate:terminal",
    "generate:pixel": "node scripts/analyze-profile-image.js",
    "generate:terminal": "node scripts/make-terminal-svg.js",
    "update:date": "node scripts/update-readme-date.js"
  }
}
